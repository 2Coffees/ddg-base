This is a swift implementation of the popular Chinese card game 斗地主(landlord duel)

To build, just ` git clone ` the repo and run ` swift build `.
To run, just do ` swift run `.

This project was made on windows. I know. Swift on windows. Ridiculous.
